# Phase 0 — Foundations

Status: **Done**

## Checklist

- [x] Git repo created (`AtharvaN16/Hurt2Heal-Website`), `main` + `dev` branches, kept in sync
- [x] `.claude/skills/` + `.agents/skills/` vendored (mattpocock engineering/productivity skills, `impeccable`, `superpowers` plugin)
- [x] Next.js scaffold (App Router, TypeScript, Tailwind) via Bun
- [x] Placeholder routes for Home, About, Programs, Resources, Get Involved, Contact, sourced from the client's rough doc via `src/lib/content.ts`
- [x] Framer Motion, Phosphor Icons installed
- [x] MCPs: Vercel (pending auth), Figma (connected via hosted plugin)
- [x] Vercel project linked, connected to GitHub, first production deploy live at `hurt2heal-website.vercel.app`
- [x] `PROJECT.md` written (vision + rough phases)

## Notes

- CMS choice (Sanity vs. Payload vs. none) deliberately deferred to Phase 3 — the content layer is shaped so the swap is cheap later.
- Client's domain not yet connected — waiting on the actual domain name.
